packageRank 0.3.0.9016
==================

- amend/set plot.cranDownloads(pkg.version = TRUE).


packageRank 0.3.0.9015
==================

- remove packageRank(version.filter).


packageRank 0.3.0.9014
==================

- amend resolveDate().
- change/clean S3 function classes.


packageRank 0.3.0.9013
==================

- add cranDownloads(packages = c(NULL, "R")) examples to README.


packageRank 0.3.0.9012
==================

- rename packageVersions() to packageHistory().
- packageRank(filter.size, filter.version) to packageRank(size.filter, version.filter).


packageRank 0.3.0.9011
==================

- packageRank(filter.version) test.


packageRank 0.3.0.9010
==================

- copyedit README.


packageRank 0.3.0.9009
==================

- consolidate resolveFromDate() and resolveToDate() as resolveDate().


packageRank 0.3.0.9008
==================

- add resolveFromDate() and resolveToDate().


packageRank 0.3.0.9007
==================

- fix packageCRAN(package = "SoilHyP").


packageRank 0.3.0.9006
==================

- set plot.cran_downloads(pkg.version = FALSE).
- use abline(col = "red") for 'pkg.version'.


packageRank 0.3.0.9005
==================

- add package version annotation to plot.cran_downloads() prototype.
- add packageVersions(), packageCRAN() and packageArchive() prototypes.


packageRank 0.3.0.9004
==================

- enable negative filter values in packageLog() and packageRank().


packageRank 0.3.0.9003
==================

- add packageLog(filter = FALSE).
- amend README: filter and "Registered S3 method overwritten by 'R.oo'".


packageRank 0.3.0.9002
==================

- filter out package downloads < 1000 bytes via packageRank(filter = TRUE).


packageRank 0.3.0.9001
==================

- rebuild README to fix 2019-10-02 and 2019-10-17 zeros.


packageRank 0.3.0.9000
==================

- add packageLog() prototype.


packageRank 0.3.0
==================

- add cranDownloads().
  - enable "yyyy-mm-dd", "yyyy-mm" or "yyyy" in cranDownloads(from, to).
  - add plot.cran_downloads(points = "auto").
  - add plot.cran_downloads(r.version).
- deprecate cran_downloads2().
- add dayOfMonth().
- add bioconductorDownloads().
- add bioconductorRank().


packageRank 0.2.0
==================

- implement minor parallelization for packageRankTime().
- base graphics for single package/date; ggplot2 for multiple packages/dates.
- fix smooth argument in plot.package_rank_time(graphics_pkg = "base").


packageRank 0.1.0
==================

- first CRAN release.
