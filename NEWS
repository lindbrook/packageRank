packageRank 0.3.0.9043
==================

- fix "`geom_smooth()` using formula 'y ~ x'" warning for 'ggplot2' v3.3.0.


packageRank 0.3.0.9042
==================

- remove ipPackage().


packageRank 0.3.0.9041
==================

- remove emptyFolderAudit() and versionsObserved().
- rename fetchLog2() to fetchCranLog().


packageRank 0.3.0.9040
==================

- fix blog.data$smpl.
- set plot.cranDownloads(points = "auto") for <= 45 days.


packageRank 0.3.0.9039
==================

- fix blog.data$oct.data.
- fix blog.data$smpl.archive.


packageRank 0.3.0.9038
==================

- add smpl, smpl.histories, smpl.archive and smpl.archive.histories to
  blog.data.


packageRank 0.3.0.9037
==================

- add oct.data to blog.data.
- add cholera.data, ggplot2.data, and VR.data to blog.data.


packageRank 0.3.0.9036
==================

- use fetchLog2() in packageLog() and packageRank().


packageRank 0.3.0.9035
==================

- add countryPackage().
- add ipPackage().
- add fetchLog2().

packageRank 0.3.0.9034
==================

- add versionObserved().


packageRank 0.3.0.9033
==================

- format downloads in packageRank().


packageRank 0.3.0.9032
==================

- add packageCountry().


packageRank 0.3.0.9031
==================

- add blog.post data.
- set plot.cranDownloads(points = "auto") for <= 62 days.


packageRank 0.3.0.9030
==================

- add plot.cranDownloads(package.version = FALSE): last version release.
- move packageRankTime() to plot.cranDownloads(poulation.plot = FALSE).
- add populationPlot().


packageRank 0.3.0.9029
==================

- fix packageRank(): put stop() before warning().


packageRank 0.3.0.9028
==================

- add fixDate_2012().
- use fixDate_2012() in packageLog() and packageRank().


packageRank 0.3.0.9027
==================
- packageHistory(): clean output row.names.


packageRank 0.3.0.9026
==================

- ensure packageLog() sorts by time.
- use packageLog(packages = NULL) to view day's log.


packageRank 0.3.0.9025
==================

- order packageArchive() output by date.


packageRank 0.3.0.9024
==================

- specify URL for available.packages().
- amend/use check.cran & check.archive arguments.


packageRank 0.3.0.9023
==================

- add validatePackage().
- cranDownloads() and packageRank(): validate packages.


packageRank 0.3.0.9022
==================

- remove cranDownloads(pkg_version) prototype code.


packageRank 0.3.0.9021
==================

- archivePackages(): update empty folders.
- add emptyFolderAudit().


packageRank 0.3.0.9020
==================

- fix archivePackages() parLapply() code.


packageRank 0.3.0.9019
==================

- archivePackages(): move README test.
- archivePackages(): update empty folders.


packageRank 0.3.0.9018
==================

- add/amend cranDownloads(validate.packages = FALSE, include.archive = TRUE).


packageRank 0.3.0.9017
==================

- validate package names in cranDownloads().
- add archivePackages() prototype.


packageRank 0.3.0.9016
==================

- amend/set plot.cranDownloads(pkg.version = TRUE).


packageRank 0.3.0.9015
==================

- remove packageRank(version.filter).


packageRank 0.3.0.9014
==================

- amend resolveDate().
- change/clean S3 function classes.


packageRank 0.3.0.9013
==================

- add cranDownloads(packages = c(NULL, "R")) examples to README.


packageRank 0.3.0.9012
==================

- rename packageVersions() to packageHistory().
- packageRank(filter.size, filter.version) to packageRank(size.filter, version.filter).


packageRank 0.3.0.9011
==================

- packageRank(filter.version) test.


packageRank 0.3.0.9010
==================

- copyedit README.


packageRank 0.3.0.9009
==================

- consolidate resolveFromDate() and resolveToDate() as resolveDate().


packageRank 0.3.0.9008
==================

- add resolveFromDate() and resolveToDate().


packageRank 0.3.0.9007
==================

- fix packageCRAN(package = "SoilHyP").


packageRank 0.3.0.9006
==================

- set plot.cran_downloads(pkg.version = FALSE).
- use abline(col = "red") for 'pkg.version'.


packageRank 0.3.0.9005
==================

- add package version annotation to plot.cran_downloads() prototype.
- add packageVersions(), packageCRAN() and packageArchive() prototypes.


packageRank 0.3.0.9004
==================

- enable negative filter values in packageLog() and packageRank().


packageRank 0.3.0.9003
==================

- add packageLog(filter = FALSE).
- amend README: filter and "Registered S3 method overwritten by 'R.oo'".


packageRank 0.3.0.9002
==================

- filter out package downloads < 1000 bytes via packageRank(filter = TRUE).


packageRank 0.3.0.9001
==================

- rebuild README to fix 2019-10-02 and 2019-10-17 zeros.


packageRank 0.3.0.9000
==================

- add packageLog() prototype.


packageRank 0.3.0
==================

- add cranDownloads().
  - enable "yyyy-mm-dd", "yyyy-mm" or "yyyy" in cranDownloads(from, to).
  - add plot.cran_downloads(points = "auto").
  - add plot.cran_downloads(r.version).
- deprecate cran_downloads2().
- add dayOfMonth().
- add bioconductorDownloads().
- add bioconductorRank().


packageRank 0.2.0
==================

- implement minor parallelization for packageRankTime().
- base graphics for single package/date; ggplot2 for multiple packages/dates.
- fix smooth argument in plot.package_rank_time(graphics_pkg = "base").


packageRank 0.1.0
==================

- first CRAN release.
